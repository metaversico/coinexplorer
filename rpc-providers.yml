# RPC Provider Configuration
# This file defines the available RPC providers for different blockchain networks

providers:
  - name: solana-mainnet-beta
    chain: solana-mainnet
    url: https://api.mainnet-beta.solana.com
    interval: 5000 # 5 seconds
    
  - name: helius-project1
    chain: solana-mainnet
    url: https://mainnet.helius-rpc.com/?api-key=YOUR_API_KEY_HERE
    interval: 3000 # 3 seconds
    
  - name: solana-devnet
    chain: solana-devnet
    url: https://api.devnet.solana.com
    interval: 1000 # 1 second

# Default settings
defaults:
  max_calls_per_provider: 3
  max_executions_per_interval: 3
  target_chain: solana-mainnet